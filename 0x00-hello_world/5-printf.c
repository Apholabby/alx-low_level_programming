#include <stdio.h>
/**
 * main - prints out to stdout
 * Description: this programm prints a string
 * Return: ends program
 */
int main(void)
{	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
