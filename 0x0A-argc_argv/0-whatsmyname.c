#include <stdio.h>
/**
 * main - entry point
 * @argc: size of argv
 * @argv: name of program and other arguments
 * Return:always o (succsess)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
